let x; 
let espacio = 10;

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  
  x = 50; // X siempre arranca en 50!
  
  line(x,0,x,height);
  
  x = x + espacio;

  line(x,0,x,height);
  
  x = x + espacio;
  line(x,0,x,height);
  
  x = x + espacio;
  line(x,0,x,height);
  
  x = x + espacio;
  line(x,0,x,height);
  
  x = x + espacio;
  line(x,0,x,height);
  
  x = x + espacio;
  line(x,0,x,height);
  
  x = x + espacio;
  line(x,0,x,height);
  
  x = x + espacio;
  line(x,0,x,height);
  
  x = x + espacio;
  line(x,0,x,height);


}