
function setup() {
    createCanvas(500, 250)
    textAlign(CENTER);
    textSize(32);
  }
  
  function draw() {
    background(0, 255, 255)
  
     if (mouseIsPressed ){
        text("Estoy presionando el mouse!", 250,125)
    
    }
  }
  
  